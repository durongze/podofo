{
    // CMAKE_BUILD_TYPE
    "tasks": [
        {
            "type": "cppbuild",
            "label": "C/C++: g++ 生成活动文件",
            "command": "cmake",
            "args": [
                "--build",
                "${workspaceRoot}/dyz"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$gcc"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "detail": "调试器生成的任务。"
        }
    ],
    "version": "2.0.0",
    "options": {
        "cwd": "${workspaceRoot}/"
    },/////////////////////////////////////////////////指定命令执行所在路径
}